{
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "installCommand": "npm install --production",
    "buildCommand": ""
  },
  "routes": [
    { "src": "/(.*)", "dest": "index.js" }
  ]
}
