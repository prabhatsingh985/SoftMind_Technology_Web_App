{
  "version": 2,
 
  "env": {
    "NODE_ENV": "production"
  },
 
  "installCommand": "npm install --production --force",
 
  "buildCommand": "npm run build",
 
  "routes": [
    { "src": "/(.*)", "dest": "index.js" }
  ]
}
